def print(x) {}
def read() {}

def fibtail(n, res0, res1) {
	if n == 0 {
		res1
	} else {
		fibtail(n-1, res1, res0+res1)
	}
}

def fib(n) {
	if n < 2 {
		1
	} else {
		fib(n-1)+fib(n-2)
	}
}

def launch(n) {
	print(fibtail(n, 0, 1))
	print(fib(n))
}

def main() {
	for i = 1, i < 40, i=(i+1) {
		print(fib(i))
	}
}
