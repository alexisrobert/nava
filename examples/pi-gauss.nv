def print(double x) {}
def sqrt(double x) {}

def pi(double n) {
	double a:=1
	double b:=(1/sqrt(2))
	double t:=(1/4)
	double p:=1

	for i = 0, i<n, i=(i+1) {
		double a2:=((a+b)/2)
		b=(sqrt(a*b))
		t=(t-(p*(a-a2)*(a-a2)))
		p=(2*p)
		a=a2
	}

	((a+b)*(a+b)/(4*t))	
}

def main() {
	print(pi(3))
}
